<%= link_to 'All Books', '/books' %>
<%= link_to 'All Authors', '/authors' %>
<%= link_to "New Author", "/authors/new" %>

<% @authors.each do |author|%>
  <h3><%= author.name  %></h3>
  <p><%= author.created_at %></p>
  <%= link_to "Update #{author.name}", "/authors/#{author.id}/edit" %> 
<% end %>
